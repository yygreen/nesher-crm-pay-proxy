# Build doc — Nesher Mercury Pay day (2026-08-05)

> For Google Drive "Build Docs / JRM Hotels" — Drive connector was disconnected at write
> time; paste this doc there and add a newest-first line to the INDEX doc.

## What shipped (all live on crm.flynesher.com + www.flynesher.com)

1. **Pay modal redesign v2** (`inject.js`, commits 8badb47 + 56940be) — summary strip,
   hero amount, customer block, structured invoice preview (line table + booking facts +
   travelers/flights/services + collapsible memo), one calm banner + field-level hints,
   Advanced collapsed, dedicated success panel (Copy / Open / Done), loading skeleton,
   focus trap, mobile sticky footer, teal #0f766e.
2. **Customer-grade invoice memo** (`draft.js`) — reads like a booking confirmation; CRM
   ids / price-source / email echo moved to Mercury `internalNote` (org-only), PNR in
   `poNumber`, stay/flight range in `servicePeriodStart/EndDate`.
3. **"OUR EMAIL" clarity** — amber chip + explicit warning whenever the email is a
   placeholder or any @jrmhotels.com / @flynesher.com address; clears live on edit.
4. **Auto-fill widening** (commit 7147897, verified against live DB) — hotels fall back
   to `offer.hotel_price` (ILS x1.03 spot, "no markup" advice); USD payments in
   `core_jrmhotelpayment` deduct from the auto amount (req#87: $554.65 paid → $0 due);
   Quoted/Paid/Due strip in modal + memo. Reservations already auto-filled 39/40;
   customer emails genuinely absent in CRM (44/211) — placeholder flow is the ceiling.
5. **Invoice cleanup** — 27 pre-8/05 unpaid never-sent Mercury invoices cancelled on
   Joseph's order (guardrails: kept everything created 8/05 + anything sent to a
   customer; verified 0 links ever sent via WhatsApp CRM, 1 via email = kept JRM-190-O48).
6. **nesher-links.josephgreen.ai retired** — replaced with a "use the CRM Mercury Pay
   button" notice so nobody sends a dead link.

## Verification

- 46/46 tests green; emitted browser script syntax-checked; contract check (all IDs /
  POST keys / API paths / idempotent injection).
- 6 rendered states screenshotted (desktop + true-390px mobile).
- `/__nesher_pay/health` now returns a `build` tag — deploy verification without login.
- New draft logic run read-only against the live CRM DB before deploy.
- Post-cancel Mercury state: 4 invoices remain (2 paid, 2 unpaid — all created 8/05).

## Deploy

GitHub push does NOT reliably auto-deploy this Railway service. Reliable path:

```
cd C:\Users\User\nesher-crm-pay-proxy
npm test
git add <files> && git commit && git push origin main   # repo truth
$env:RAILWAY_TOKEN = <RAILWAY_PROJECT_TOKEN from ~/.api-keys.env>
railway up --service 964d731e-6768-4a49-95fd-16799d910c47 --environment d6681e4d-5562-4f52-b560-175689ccfb00 --detach
curl https://crm.flynesher.com/__nesher_pay/health   # expect the new build tag
```

## Open items

- ~13 booking@ drafts still contain the cancelled pay links — delete/refresh before any send.
- Nesher-Payment-Watch may emit a burst of status-change alerts (27 cancels) to joseph@.
- Mercury dashboard branding (Invoicing → Invoice settings → logo / color / company name)
  — 2-minute manual task, includes the "Air Today Travel Inc" display-name question.
- Yiddish voice-note → English transcription in the WhatsApp portal: plan approved shape,
  awaiting "build it".
